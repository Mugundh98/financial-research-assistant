# ---- Core / contracts ----
pydantic>=2.7
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27

# ---- Retrieval (vector + keyword + hybrid) ----
scikit-learn>=1.4
rank-bm25>=0.2.2
numpy>=1.26
pandas>=2.2

# ---- Knowledge graph (lighter-fidelity component) ----
networkx>=3.2

# ---- LLM backend (default = Anthropic Claude; mock fallback needs no key) ----
anthropic>=0.39

# ---- API + dashboard deployment ----
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.33

# ---- Evaluation + tests ----
pytest>=8.0

# Optional upgrade for real semantic embeddings (falls back to TF-IDF/LSA if absent):
# sentence-transformers>=2.6
